import React from 'react';
import { Typography } from '@mui/material';

const NotFound = () => {
  return (
    <Typography variant="h4" component="h4" pt={8} color="gray" textAlign="center">
      This Page does not exist
    </Typography>
  );
};

export default NotFound;
